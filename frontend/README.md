# Bible Reference AI — frontend

Next.js chat UI for the FastAPI backend in `../backend`. The `/api/query` route handler proxies to `BACKEND_URL` so the backend address is never exposed to the browser.

See the [root README](../README.md) for the full architecture and setup.
